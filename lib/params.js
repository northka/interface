/**
 * Created by chenchaochao on 17/1/2.
 */
function resolveParam(opt,query,body){
    let parameter = opt.parameter,
        type      = opt.type,
        regexp    = opt.regexp,
        fromUrl   = opt.fromUrl

}